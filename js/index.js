import { renderBlogCard } from './data/renderBlogCard.js'; // import renderBlogCard function from renderBlogCard.js

document.addEventListener('DOMContentLoaded', () => {
    renderBlogCard(); // Render the blog cards
});


